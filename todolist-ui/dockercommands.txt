docker network create todo-net

docker run -d `
  --name db `
  --network todo-net `
  -e POSTGRES_USER=postgres `
  -e POSTGRES_PASSWORD=postgres `
  -e POSTGRES_DB=tododb `
  -p 5432:5432 `
  -v pgdata:/var/lib/postgresql/data `
  postgres:17-alpine

cd TodoApi
docker build -t todoapi-backend .
docker run -d `
  --name backend `
  --network todo-net `
  -p 5000:5000 `
  todoapi-backend

cd ..
cd todolist-ui
docker build -t todoapi-frontend .
docker run -d `
  --name frontend `
  --network todo-net `
  -p 3000:80 `
  todoapi-frontend
cd ..

# lab56 - test1-1-1-1-1

docker network create todo-network

docker pull timadyakonu/todo-backend:latest
docker pull timadyakonu/todo-frontend:latest

docker run -d --name db --network todo-network -e POSTGRES_USER=postgres -e POSTGRES_PASSWORD=postgres -e POSTGRES_DB=tododb -p 5432:5432 postgres:17-alpine

docker run -d --name backend --network todo-network -e ConnectionStrings__DefaultConnection="Host=db;Port=5432;Database=tododb;Username=postgres;Password=postgres" -p 5000:5000 timadyakonu/todo-backend:latest

docker run -d --name frontend --network todo-network -p 3000:80 timadyakonu/todo-frontend:latest